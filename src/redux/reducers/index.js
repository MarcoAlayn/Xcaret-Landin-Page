// import the actions defined

const initialState = {};

const rootReducer = (state = initialState, action) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
export default rootReducer;
